services:
  # Single web service that builds and serves both frontend and backend
  - type: web
    name: naksh-jewels
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
    healthCheckPath: /health
